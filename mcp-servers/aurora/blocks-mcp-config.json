{
  "command": "npx",
  "args": [
    "mcp-remote",
    "https://ai.aur.lu/v1/mcp",
    "--header",
    "Authorization:Bearer ${AURORA_API_KEY}"
  ],
  "env": {
    "AURORA_API_KEY": "${env:AURORA_API_KEY}"
  }
}
